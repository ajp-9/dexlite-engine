#pragma once

#include "../Entity/Entity.hpp"

namespace dex
{
    namespace Component
    {
        /*struct Base
        {
            Base(const Entity& own_entity)
                : OwnEntity(own_entity)
            {}
        public:
            Entity OwnEntity;
        public:
            friend class dex::Entity;
        };*/
    }
}
