#pragma once

// make other comp names after like shader3d

/*#include "../Entity/Entity.hpp"

namespace dex
{
    namespace Component
    {
        struct Base
        {
            Base() = default;

            Base(Entity entity)
                : m_Entity(entity)
            {}
        protected:
            Entity m_Entity;
        };
    }
}*/
